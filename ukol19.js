"use strict";
switch(a) {
    case (a !== 1):
        break;
    case (b < 5 && (x-y) === 20):
        console.log('vysledek 1');
        break;
    case (c > 3 && d(e) === f):
        console.log('vysledek 2');
        break;
    case (c > 3):
         console.log('vysledek 3');
        break;
}